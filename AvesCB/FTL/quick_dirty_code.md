# Welcome
## Perl Customs

```perl
@STUB-@FIXME-@UPDATE-@PATCH-@DETAIL-@RELAY-@UNIT32-@SDK_KIT- 
open system import      #*# REV. 3/11
import open system      #*# OSS
import system caller    #*# SYSTEM CALLER IMPORTS
fetch caller $Program   #*# FETCH <do> <C> $Program
return $Manifest via()  #*# Return $Manifest via CB Function
prefetch() connect.map* #*# prefetch function connect.map*
use readonly ;;;        #*# use readonly full stop.
{'https://www.github.com/codeR32-dev'}
use __PKG__ use strict use warnings 
do <C> %Array->$Scalar #*# Key Daytime Data
load() ^Carets^  {...} #*# Peek Carets Look
load() &Routine  {...} #*# Running Routine
load() *wildcode {...} #*# Wild Code Words
BEGIN: precode %table_code via() grab 11-99
postfix=>special for special->route 
my mode do <C> for :y: [goto] @ID 
for my ID [seek] @peek 
print{"NEWID"} for @peek {'.src'} + {'.path'} == {'.code'}
!==require open system print{"$Manifest"}
@Require open @service system 
&& :|: @support @repair &Retry {...} my \ref => open system
say open @services :|: @support 
system @Hummit_ID !==require Hummit_ID=={'.src++@RTV%KeyCode=>self->caller'}
for my :S: connect caller->self
shift(@PASS:{'****'} && @ALERT:{'****'})
do my <C> connect.map[manifest require]+[keycode]+
[Hummit_ID]+[(C):O:{D}:E:->add.this*Y*]==[require manifest] 
map.connect via() connect mapper() mapper() 
connect via :S: for :S: [select]
@mode pass: 1001 value: 1001 return 1 END
```
## Thanks
