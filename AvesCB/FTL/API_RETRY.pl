#!/perl V.build => %32.CB
!== require system caller \@read and &&|| or \@write $ROI;
@import @ENV @system;
use @read;
use @strict;
use @warnings;
use @system @ENV;
for :[select]:+:[@Mode]:=:[@connect]:
:[@connect]:=:[@Mode]:+:[select]: for *my* $Code;
!== require {*API*}
for if {
	next {$available.code*}
	*API* caller stat system;       {*CB*=*Window*+*API*}
	else  caller stat system *API*; {*API*+*Window*=*CB*}
	@pass: @value;
	@value: @pass;
}
@open next $available.question[=?]
@via->return=>$Path_Route_API={'~//RTV32'}
else {
	@retry() goto $path->@Route;
}
@try->[@index]+[@connect]=[@map]
else {
	@retry() goto $Route->@path;
}
@close for $additional $condtional [@result.connect.value]
for [value.connect.result*]
sub console {
	for if [*result.connect.value]
	else @Retry import $OPTS_FILES seek {'via+cpan==*net*'}
}
@pass: $value;
@value: $pass;
return 1;
return $result;
use exit
	exit()
	!exit
	return {pass: 0; value: close;}
@END; {\fi;;;}
