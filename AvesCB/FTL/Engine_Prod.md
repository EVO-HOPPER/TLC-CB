# Welcome
## Main Menu

```perl
#!/perl
use readonly;
import system;
BEGIN
ref. goto @readonly;
@AVES->$Command_Tree goto next <C> for \my => sub Command {
   \(( 						          !== require import connect open system;   # 99
   	\~~ 					          !== require use AIO             STREAM;   # tilde
 	   \!! 					          !== require use STATEMENT       DECLARE;  # 11
  	  \@@					          !== require use BUILD           VERSION;  # 22
   	   \##					        !== require use perl            comments; # 33
      	\$$					        !== require use scalar 			    @Check;   # 44
     	   \%%				        !== require use Hash 			      @Check;   # 55
      	  \^^				        !== require use Directional    	@Check;   # 66
       	   \&&				      !== require use Routines 		    @Check;   # 77
            \**				      !== require use Rand Gen Star 	@Check;   # 88
		 	       \!				      !== require use Statement 		  token;    # 1
	 	  	      \@			      !== require use Build Version 	token;    # 2
	  	   	     \#			      !== require use comment 		    token;    # 3
	   		      	\$			    !== require use Scalar 			    token;    # 4
		     	       \%			    !== require use Hash 			      token;    # 5
	     	  	      \^		    !== require use Directional 	  token;    # 6
		  	   	       \&		    !== require use Routines 		    token;    # 7
		  	    	      \*		  !== require use Asterisk 		    token;    # 8
		         	       \()	  !== require use Parameter 		  token;    # 90
		     	  	        \!)	  !== require use Statement       close;    # 10
     		  	   	        \))	!== require use Close           close;    # 00
pass: value;
value: pass;
}
return 1;
END;
```
## Thanks
